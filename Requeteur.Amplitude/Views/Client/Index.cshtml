@model List<Client>  
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor  
@{  
    ViewData["Title"] = "Clients";  
}  

@* <div class="d-flex justify-content-between align-items-center mb-3">  
    <h1 class="h3">Liste des clients</h1>  
    <a class="btn btn-primary" asp-action="Create"><i class="bi bi-plus"></i> Nouveau client</a>  
    <a class="btn btn-primary" asp-action="Create"><i class="bi btn-icon-split"></i> Exporter</a>
</div>   *@
<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h3 mb-0">Liste des clients</h1>
    <div class="d-flex gap-2">
        <a class="btn btn-primary" asp-action="Create"><i class="bi bi-plus"></i> Nouveau client</a>
        <a class="btn btn-primary" asp-controller="Client" asp-action="Export"><i class="bi bi-download"></i> Exporter</a>
    </div>
</div>

<form method="get" class="mb-3">  
    <div class="input-group">  
        <input type="text"  
               name="search"  
               class="form-control"  
               placeholder="Rechercher un client..."  
               value="@HttpContextAccessor.HttpContext.Request.Query["search"]" />  
        <button class="btn btn-outline-secondary" type="submit"><i class="bi bi-search"></i> Rechercher</button>  
    </div>  
</form>  

<table class="table table-striped table-bordered table-responsive">  
    <thead class="table-dark">  
        <tr>  
            <th>CodeAgence</th>
            <th>IDClient</th>
            <th>NumeroMembre</th>
            <th>TypeClient</th>
            <th>Nom</th>
            <th>Prénom</th>
            <th>RaisonSociale</th>
            <th>Sigle</th>
            <th>AdresseMail</th>
            <th>Sexe</th>
            <th>DateDeNaissance</th>
            <th>VilleDeNaissance</th>
            <th>NumeroPieceIdent</th>
            <th>DateDelivrancePiece</th>
            <th>DateDelivrancePiece</th>
            <th>LieuDelivrancePiece</th>
            <th>DateDeMission</th>
        </tr>  
    </thead>  
    <tbody>  
        @foreach (var clt in Model)  
        {  
            <tr>  
                <td>@clt.CodAge</td>  
                <td>@clt.IdClient</td>  
                <td>@clt.NumeroMembre</td>  
                <td>@clt.TypeClient</td>  
                <td>@clt.Nom</td>  
                <td>@clt.Prenom</td>  
                <td>@clt.RaisonSociale</td>  
                <td>@clt.Sigle</td>
                <td>@clt.AdresseMail</td>
                <td>@clt.Sexe</td>
                <td>@clt.DateDeNaissance</td>
                <td>@clt.VilleDeNaissance</td>
                <td>@clt.NumeroPieceIdent</td>
                <td>@clt.DateDelivrancePiece</td>
                <td>@clt.LieuDelivrancePiece</td>
                <td>@clt.DateDeMission</td>
                <td>  
                    <a class="btn btn-sm btn-info" asp-action="Details" asp-route-id="@clt.IdClient"><i class="bi bi-eye"></i></a>  
                    <a class="btn btn-sm btn-warning" asp-action="Edit" asp-route-id="@clt.IdClient"><i class="bi bi-pencil"></i></a>  
                    <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@clt.IdClient"><i class="bi bi-trash"></i></a>  
                </td>  
            </tr>  
        }  
    </tbody>  
</table>